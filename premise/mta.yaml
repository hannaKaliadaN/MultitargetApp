_schema-version: "3.2"
ID: mtx
description: Generated by Fiori Tools
version: 0.0.1
modules:
  - name: mtx-approuter
    type: approuter.nodejs
    path: mtx-approuter
    requires:
      - name: mtx_repo_runtime
      - name: mtx-uaa
      - name: mtx_connectivity
      - name: mtx-destination-service
      - name: mtx_saas-registry
        group: destinations
        properties:
          forwardAuthToken: false
          name: ui5
          url: https://ui5.sap.com
    properties:
        TENANT_HOST_PATTERN: "^(.*)-${appname}.${default-domain}"
    parameters:
        disk-quota: 256M
        memory: 256M
        routes:
          - route: https://${subdomain}-${appname}.${default-domain}
          - route: https://consumer-enunbw8b-${appname}.${default-domain}
  - name: mtx_ui_deployer
    type: com.sap.application.content
    path: .
    requires:
      - name: mtx-repo-host
        parameters:
          content-target: true
    build-parameters:
        build-result: resources
        requires:
          - artifacts:
              - premise.zip
            name: premise
            target-path: resources/
  - name: premise
    type: html5
    path: premise
    build-parameters:
      build-result: dist
      builder: custom
      commands:
        - npm install
        - npm run build:cf
      supported-platforms: []
resources:
  - name: mtx_repo_runtime
    type: org.cloudfoundry.managed-service
    parameters:
        service: html5-apps-repo
        service-plan: app-runtime
  - name: mtx-uaa
    type: org.cloudfoundry.managed-service
    parameters:
      path: ./xs-security.json
      service: xsuaa
      service-name: mtx-xsuaa-srv
      service-plan: application
  - name: mtx-repo-host
    type: org.cloudfoundry.managed-service
    parameters:
      service: html5-apps-repo
      service-plan: app-host
  - name: mtx_connectivity
    type: org.cloudfoundry.managed-service
    parameters:
        service: connectivity
        service-name: mtx-connectivity-service
        service-plan: lite
  - name: mtx_saas-registry
    type: org.cloudfoundry.managed-service
    parameters:
      service: saas-registry
      service-name: ${appname}-saas-registry
      service-plan: application
      config:
        xsappname: mtx
        appName: mtx-approuter
        displayName: App
        description: App
        category: Custom Apps
        appUrls:
          onSubscription: https://${subdomain}-${appname}.${default-domain}/callback/v1.0/tenants/{tenantId}
          getDependencies: https://${subdomain}-${appname}.${default-domain}/callback/v1.0/dependencies
  - name: mtx-destination-service
    type: org.cloudfoundry.managed-service
    parameters:
      config:
        HTML5Runtime_enabled: false
        init_data:
          instance:
            destinations:
            - Authentication: NoAuthentication
              Name: ui5
              ProxyType: Internet
              Type: HTTP
              URL: https://ui5.sap.com
            existing_destinations_policy: update
        version: 1.0.0
      service: destination
      service-name: mtx-destination-service
      service-plan: lite
parameters:
  deploy_mode: mtx-approuter
  appname: mtx-approuter
  subdomain: us-east-va-q1fgjfxl
